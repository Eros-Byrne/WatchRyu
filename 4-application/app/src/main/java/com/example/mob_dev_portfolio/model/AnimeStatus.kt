package com.example.mob_dev_portfolio.model

/**
 * Enum for the tracking status of an anime.
 */
enum class AnimeStatus {
    AIRING,         // Used for the global discovery list
    WATCHING,
    COMPLETED,
    DROPPED,
    PLAN_TO_WATCH
}
